<!-- inside app/views/home/index.html.erb -->
<div id = "wrapper">
  <div class="card border-light mb-3" style="max-width: 50rem; padding-bottom: 50px;">
    <div class="card-header">Welcome, this is the home page</div>
    <div class="card-body">
      <h5 class="card-title">Here is the weather in your location</h5>
      <p class="card-text"> Location: <%= @country %>, <%= @city %></p>
      <p> <%= "The temperature is " + @temp.to_s + " degrees celsius" %></p>
    </div>
  </div>
</div>